export const metadata = {
  title: 'Pipeline — Matthew Golia',
  description: 'Job search dashboard'
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
